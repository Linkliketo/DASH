# V0 快速验证测试报告

> 日期：2026-07-24 · 执行：dash coder · 来源路线：`testRoad.md`（PR §一 V0）

## 结论：V0 双组件均验证通过 ✅

| 组件 | 版本 | 部署方式 | 验证结果 |
|------|------|---------|---------|
| **SysMocap**（全身动捕） | v0.8.0 Windows x64 免安装包（274MB） | 官方 Release 解压即用 | ✅ **真实摄像头端到端跑通**：MediaPipe Holistic 全身+双手+面部网格实时检测，VRM 模型（Ashtra）同步驱动，Render/Mocap 均 ~13 FPS（i7-10750H 无 GPU 加速场景） |
| **miniface**（面部微表情） | 0.1.0 源码运行 | Node 22 + react-scripts，localhost:3000 | ✅ 页面渲染正常，MediaPipe FaceLandmarker（WASM/XNNPACK）初始化成功，BlendShape 虚拟形象系统就绪（模拟摄像头验证） |

## 关键证据截图

- `shot_sysmocap_02.png`：真实摄像头画面 + 骨骼/手部/面部关键点叠加 + VRM 角色同步举手
- `shot_miniface_01.png`：浏览器内 3D 虚拟形象 + 面部追踪 "All set!" 状态

## 踩坑记录（复现时注意）

1. **GitHub 直连被重置** → 需走系统代理 `127.0.0.1:10090`（git/curl 设 `https_proxy`）
2. `npm install` 默认跳过 devDependencies → 需 `npm install --include=dev`（react-scripts 在 devDeps）
3. Node ≥17 跑 CRA5 需 `NODE_OPTIONS=--openssl-legacy-provider`
4. 无 7-Zip 且 py7zr 不支持 BCJ2 → 装 7-Zip 官方版解压
5. Chrome/Electron CDP 需 `--remote-allow-origins=*`（或 websocket `suppress_origin=True`）

## 对 DASH 的确认与发现

- ✅ "摄像头→虚拟角色"端到端概念验证通过（零成本、零 GPU）
- ✅ SysMocap 数据源/转发面板确认存在（后续可测 OSC/WebSocket→Unity 链路，S4 预演）
- 📌 **实测数据点**：MediaPipe Holistic 全身模式在本机约 13 FPS——低于文档声称的 30–60 FPS，P1-2 CPU 方案设计时需以实测为准
- 📌 SysMocap 按 R 键可录制动作数据，可用于自采集 500 样本的辅助工具

## 运行方式（下次复现）

```bash
# SysMocap
D:\DASH\V0FastTest\SysMocapApp\SysMocap-win32-x64\SysMocap.exe

# miniface（生产版）
cd D:\DASH\V0FastTest\facial-motion-capture
npx serve -s build -l 3000   # → http://localhost:3000（不要用 npm start）
```

## 双源融合（V0+，2026-07-24 深夜完成）✅

### 架构

```
SysMocap（真摄像头）→ 内置转发 socket.io :8080 ──┐
                                                  ├→ fusion/server.mjs → ws :8766 统一融合流
miniface（打补丁）  → wsBridge.ts  ws :8765 ────┘   {type:"dash-v0-fused", pts, syncErrorMs, body, face}
```

- **miniface 补丁**：新增 `src/wsBridge.ts`（自动重连 WebSocket 推送 ARKit blendshapes+头部旋转）；`FaceTracking.tsx` 第 239 行注入 `sendFaceFrame()`
- **融合服务器**：`fusion/server.mjs`（订阅两路 → 30Hz 融合广播 + FPS/同步误差统计）
- **实测**：body 7.5–8.5 FPS + face 2.5–3 FPS → fused 22 FPS，syncErr 3–334ms

### 关键坑：CRA 开发服务器导致"只能收第一帧"

症状：模型只收到第一帧后卡死，报 `importScripts ... react-refresh ... chunk.js failed to load`。
根因：webpack HMR 重编译后，faceWorker 仍引用已失效的旧 chunk → worker 崩溃。
**修复：必须用生产构建**——`npm run build` + `npx serve -s build -l 3000`（禁止再用 `npm start` 开发模式跑融合）。

### 稳定启动顺序

```bat
D:\DASH\V0FastTest\start.bat   # 菜单：1=全身(SysMocap) / 2=面部(miniface) / 3=融合
```

- **模式3 融合**：`start.bat` 自动启动融合服务器 + 融合 Viewer（http://localhost:8081/viewer），采集端需手动开 SysMocap（设置→启用转发→开始）和 miniface（双摄像头）
- **融合 Viewer**：`viewer/index.html`（three.js + @pixiv/three-vrm），订阅 ws://127.0.0.1:8766，驱动 VRM 骨骼 + 面部 BS，含自动重连

### 资源占用实测（重要）

双源同跑 + Chrome + 融合服务器时，SysMocap 从 13 FPS 降至 6–8 FPS（CPU 争抢）。**真机双人采集建议分两台机器或降一路分辨率**。

## 后续手动项（需真人/引擎环境）

- [ ] 真人测试 miniface 面部 BlendShape 精度与 GLB 动画导出
- [ ] 融合模式真人双摄像头实测（start.bat 模式3）
- [ ] 融合流 :8766 → Unity/Unreal 接收驱动实测（S4 预演，替代 Web viewer）

## 单视频源模式（替代双摄像头）✅

思路：**一个视频文件喂两路**，SysMocap 生成关节、miniface 生成面部 BS，均送入融合服务器由 viewer 渲染。

| 环节 | 操作 |
|---|---|
| 视频→y4m（miniface 用） | `python tools\video_to_y4m.py 你的.mp4 输出.y4m 640x480 30 5 1`（5秒≈67MB；y4m 无压缩，秒数=体积） |
| SysMocap 用原视频 | 动作捕捉 → 数据源选「视频文件」→ 选你的.mp4 → 设置开转发 → 开始 |
| miniface 用视频当摄像头 | `face_from_video.bat 你的.y4m`（Chrome 假摄像头） |
| 融合 | `start.bat` 选 3（融合服务器+viewer） |

**已实测**：合成源（模式4）body+face → fused 20+ FPS；视频→miniface 链路通（清晰人脸时 face 3 FPS）。
**注意**：① openpose 示例视频人脸太小，FaceLandmarker 检测不稳定，真人测试请用近景人脸视频；② 两路需同一视频，时间戳相近才同步，融合流 syncErr 可观察。

## 链条3 完整验证记录（2026-08-13，逐步排查后打通）✅

单视频源全链路：**视频 → SysMocap(关节) + miniface(面部) → 融合服务器 → viewer 渲染**，已逐跳实测打通。

| 跳 | 关键点 | 结果 |
|---|---|---|
| 视频→SysMocap | 动作捕捉→源选「视频文件」；转发开关 `设置→启用转发`；点开始后 :8080 才监听 | ✅ body 10 FPS |
| 视频→miniface | Chrome `--use-file-for-fake-video-capture=xxx.y4m` | ✅ face 3 FPS |
| →融合服务器 | 双路汇总 :8765/:8080 → 广播 :8766 | ✅ fused 22 FPS |
| →viewer | three-vrm 驱动 VRM | ✅ 模型正常运动 |

**排查中发现的 4 个坑（已修）：**
1. SysMocap 视频模式：开始按钮检查的是 Vue 状态 `window.sysmocapApp.videoPath`（不是 localStorage）；只写 localStorage 会触发 `alert("noVideoSelected")` 阻塞。真人操作直接点「选择视频文件」即可，无需脚本。
2. SysMocap `riggedPose` 真实结构：四肢/手是 `{x,y,z}` 直存（**只有 Hips 有 `.rotation`**），且**没有 Chest/Neck/Head**。
3. viewer 原本读 `P[k].rotation` → 对直存骨骼报 `undefined.x` 异常 → 已改 `P[k].rotation || P[k]`。
4. 转发服务器(:8080)仅在动捕运行时才监听；转发开关状态持久化。
