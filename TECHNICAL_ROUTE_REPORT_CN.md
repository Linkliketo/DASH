# DASH 技术路线报告

项目：DASH —— 面向游戏数字人的轻量化动作-微表情联合建模方法研究
内容：项目范围、已完成的验证、实测数据、选定的技术路线
版本：2.0 | 日期：2026-09-23 | 状态：内部阶段报告

---

## 一、项目定义

DASH 的目标是在单台消费级 CPU 机器上实现一条实时管线，把单目 RGB 视频转换成游戏数字人的动画参数：

```
视频  ->  身体骨骼  +  面部表情  ->  虚拟角色
```

约束是硬性的：消费级硬件上的实时运行，即 30 FPS 下 33 ms 的帧预算。这让模型压缩
（量化、剪枝、运行时选择）成为**第一等的研究课题**，而不是可选的优化项。

当前工作由两个问题驱动：

1. 感知模型能否被做到足够小、足够快，以适配这个预算？
2. 因此带来的精度损失能否被测量并给出边界？

第二个问题是本报告只引用**实测数字**、而不引用申报书里**目标值**的原因。目标值是一个主张，实测值是一份证据。

---

## 二、V0 快速验证（已完成）

### 2.1 验证了什么

V0 用两个零成本开源项目，在一台无 GPU 的笔记本上验证了端到端链路与双源架构。

| 组件 | 作用 | 技术本质 |
|---|---|---|
| SysMocap v0.8.0 | 身体动捕 | MediaPipe Holistic 关键点，再由 Kalidokit 做规则式逆运动学 |
| miniface 0.1.0 | 面部表情捕捉 | MediaPipe Tasks FaceLandmarker，神经网络直接回归 ARKit 52 维 BlendShape |
| 融合服务器（我方编写） | 桥接 | socket.io 加原生 WebSocket，输出统一数据流 |

通过核对双方源码与打包模型，确认了一个重要的架构差异：

- SysMocap 的关节由**规则式逆运动学**（Kalidokit）从关键点解算而来，**不是**端到端训练的网络。
- miniface 的表情由**神经网络直接回归**：FaceLandmarker 模型自身就输出 52 维 ARKit 系数。

因此两个项目代表了同一任务的两种不同技术路线，这对最终报告的对比章节有价值。由于只有 miniface 输出标准 ARKit 语义，融合层以 miniface 为面部权威源、以 SysMocap 为身体权威源。

### 2.2 实测结果

| 指标 | 数值 | 条件 |
|---|---|---|
| 身体帧率（SysMocap 单独运行） | 约 13 FPS | i7-10750H，无 GPU |
| 身体帧率（双源同跑） | 6–8 FPS | 与 miniface 争抢 CPU |
| 面部帧率（miniface） | 2.5–3 FPS | 假摄像头视频源 |
| 融合输出帧率 | 约 22 FPS | 33 ms 定时合并 |
| 同步误差 | 3–334 ms | 两路数据到达时刻之差 |
| 单视频源全链路 | 逐跳验证打通 | 视频 -> SysMocap 加 miniface -> 融合 -> viewer |

实测身体吞吐（约 13 FPS）显著低于上游文档声称的 30–60 FPS。**后续所有算力规划均以实测值为准。**

### 2.3 V0 期间发现的问题

| 问题 | 原因 | 处理 |
|---|---|---|
| 只收到第一帧 | webpack HMR 使旧 chunk 失效，而 face worker 仍引用它 | 跑融合必须用生产构建，禁用 CRA 开发服务器 |
| 摄像头被独占 | Windows 下一个摄像头只能被一个进程打开 | 双摄像头，或使用 OBS 虚拟摄像头 |
| SysMocap 视频模式启动被阻塞 | 开始按钮检查 Vue 状态而非 localStorage | 通过 GUI 选择视频文件 |
| viewer 读取身体数据时报错 | 四类骨骼直接存 `{x,y,z}`，只有 Hips 有 `.rotation` | 改读 `P[k].rotation \|\| P[k]` |
| 转发端口不监听 | 8080 端口仅在动捕运行期间开放 | 先启动动捕，再订阅 |

---

## 三、量化探针（阶段 0，已完成）

阶段 0 在投入任何压缩工程量之前，先回答三个是非问题，并设两天时间盒。

### 3.1 问题一：能否从 `.task` 容器中拆出独立 `.tflite`？

**结论：能。**

`.task` 文件是一个**无压缩的 ZIP 容器**（每个成员都是 `compress_type = 0`，
`compress_size == file_size`）：

| 成员 | 大小 |
|---|---|
| `face_detector.tflite` | 229,746 B |
| `face_landmarks_detector.tflite` | 2,553,590 B |
| `face_blendshapes.tflite` | 955,312 B |
| `geometry_pipeline_metadata_landmarks.binarypb` | 19,376 B |
| 成员合计 | 3,758,024 B |
| 容器开销 | 572 B |
| `.task` 文件大小 | 3,758,596 B |

成员合计加容器开销与文件大小**精确吻合**，这构成对提取结果的自我校验。证据：`quant/result/probe_unpack.md`。

### 3.2 问题二-A：拆出的 TFLite 能否转成 ONNX？

**结论：三个中只有一个。**

| 模型 | 是否转换成功 | 详情 |
|---|---|---|
| `face_detector.tflite` | 成功 | 30 个固定种子输入上的误差中位数 8.678e-05 |
| `face_blendshapes.tflite` | 失败 | `tflite2onnx` 内部抛 `TypeError: object of type 'int' has no len()` |
| `face_landmarks_detector.tflite` | 失败 | `tflite2onnx` 内部抛 `IndexError: list index out of range` |

两点结论很重要：

1. 失败原因是**转换器自身的 bug**，而不是算子不受支持。这个区分决定了该路线是否值得继续投入。
2. 转换成功的 `face_detector` 是三个模型中**最不相关的一个**：它负责在画面中定位人脸。真正产生微表情的是 `face_blendshapes`，而它转不了。**因此路线 A 没有提供项目真正需要的组件。**

转换成功的那个模型的保真度：

| 输出 | 形状 | 误差中位数 | 最坏误差 |
|---|---|---|---|
| regressors | (1, 896, 16) | 7.629e-05 | 1.221e-04 |
| classificators | (1, 896, 1) | 8.678e-05 | 1.678e-04 |

残余误差可以解释为**原始 TFLite 模型内部存在 float16 张量**（实测：`face_detector` 中 30%，
`face_blendshapes` 中 20%，`face_landmarks_detector` 中 35%）。它并非由转换逻辑引入。

调用方必须处理的布局差异：TFLite 期望 `(1, 128, 128, 3)`（NHWC），ONNX 期望 `(1, 3, 128, 128)`
（NCHW）。同一张图必须分别为两个运行时转置。

证据：`quant/result/probe_route_a.md`。

### 3.3 问题二-B：候选 B —— 自产成对数据集

由于真正需要的模型无法转换，其功能改为**用原模型生成数据、再训练一个小网络来复现**。这在原理上属于知识蒸馏，同时产出一个权重完全自有、可任意改造的模型 —— 这是后续阶段剪枝实验的前提。

数据集的生成方式是：用现有的 MediaPipe 管线跑视频，逐帧保存输入与输出对。

| 项 | 值 |
|---|---|
| 文件 | `quant/data/pairs.npz` |
| 样本数 | 1500 |
| `X`（特征） | (1500, 1434) float32，478 个关键点乘 3 个坐标，展平 |
| `Y`（标签） | (1500, 52) float32，ARKit BlendShape 分数 |
| NaN 个数 | 0 |
| 无人脸的帧 | 0（这类帧被跳过，因此特征与标签始终对齐） |

标签范围 `[0, 0.9907]`，关键点坐标范围 `[-0.0853, 1.0458]`（归一化坐标在接近画面边缘时可以略微越出 `[0, 1]`）。

证据：`quant/result/pairs_preview.png`（关键点散点、BlendShape 时序曲线、标签分布直方图）。

### 3.4 问题二-B：训练与导出 ONNX

进行中（`quant/train_mlp.py`）。目标接口：

```
输入  "landmarks"    (B, 1434) float32   原始坐标，标准化在计算图内部完成
输出  "blendshapes"  (B, 52)   float32
```

### 3.5 问题三：能否测出延迟、体积、精度三个数？

**结论：能。** 度量工具（`quant/bench.py`）先建后用**已知答案校准**：它复现了 11.7–13.2 ms，
而这个数字此前是用完全不同的方法在 `deploy/02_realtime.py` 里得到的。两把独立的尺子给出同一个答案，这是本报告中数字可信的原因。

证据：`quant/result/bench_baseline.md`。

### 3.6 决策门

| 问题 | 结论 | 证据 |
|---|---|---|
| 问题一：能拆出独立 `.tflite`？ | 是 | `probe_unpack.md` |
| 问题二：能跑通 PTQ 得到 INT8 模型？ | 是，走候选 B | `probe_route_a.md` 与 Task 6 的导出结果 |
| 问题三：能测出延迟、体积、精度？ | 是 | `bench_baseline.md` |

路线选择：**压缩工作由候选 B 承担。** 候选 A 贡献一个否定结论和一份保真度分析，两者都属于最终报告的内容。

---

## 四、实测数据汇总

以下数字全部产生于本机。硬件：Intel Core Ultra 5 255H（V0 阶段为 i7-10750H），Windows 11，仅 CPU，无 GPU。

| 量 | 值 | 来源 |
|---|---|---|
| 摄像头采集率 | 30.0 FPS | `deploy/02_realtime.py` |
| 面部管线推理耗时 | 11.7–13.2 ms | `deploy/02_realtime.py` |
| 冷启动开销 | 约 2.4 s | `deploy/02_realtime.py` |
| 基准实测平均延迟 | 11.916 ms | `quant/result/bench_baseline.md` |
| 基准 p50 / p95 / min | 11.737 / 13.981 / 10.303 ms | 同上 |
| 基准协议 | 预热 10 次，计时 100 次 | 同上 |
| 测量噪声 | 约 5%（均值）、11.6%（p95） | 重复运行 |
| `.task` 容器 | 3,758,596 B | `quant/result/probe_unpack.md` |
| 内含模型 | 3 个 `.tflite` 加 1 个 `.binarypb` | 同上 |
| 可转 ONNX 的模型数 | 3 个中 1 个 | `quant/result/probe_route_a.md` |
| 转换保真度（可转的那个） | 中位数 8.678e-05 | 同上 |
| `face_detector.onnx` 体积 | 425,659 B | `quant/models/onnx/` |
| 数据集样本数 | 1500 | `quant/data/pairs.npz` |
| 数据集形状 | X (1500, 1434)，Y (1500, 52) | 同上 |
| 从未激活的 BlendShape 通道 | 52 个中 14 个 | `quant/result/pairs_preview.png` |
| 身体动捕吞吐（V0） | 单独运行约 13 FPS | V0 报告 |
| 融合流帧率（V0） | 约 22 FPS | V0 报告 |

帧预算的算术：11.9 ms 的推理占 30 FPS 下 33.3 ms 预算的 36%。**在当前阶段，瓶颈已经在摄像头一侧**，因此量化不会提高现有管线的帧率。它的价值会出现在提高分辨率、同时运行多个模型、或迁移到更弱设备时。如实说明这一点本身就是结论的一部分。

---

## 五、技术路线

### 5.1 面部路径分两条车道

面部路径被有意拆成两条，因为两种可用技术回答的是不同问题。

| | 快车道 | 叙事车道 |
|---|---|---|
| 模型 | MediaPipe FaceLandmarker | LibreFace（ResNet-18） |
| 输出 | ARKit 52 维 BlendShape | 17 个面部动作单元（AU） |
| 体积 | 3.6 MB | 约 90 MB |
| 目的 | 把整条链路先跑通 | 可解释、可编辑、可迁移的面部表征 |
| 压缩空间 | 有限（上游已用 float16 优化过） | 大（体积是前者的 24 倍） |

快车道负责建立管线。叙事车道承载研究贡献：动作单元有解剖学定义，因此可以通过**显式模型**而非黑箱映射到 BlendShape，并且跨数据集迁移更好。

### 5.2 量化的两批对象

| 批次 | 对象 | 为什么选它 |
|---|---|---|
| 第一批 | MediaPipe TFLite 模型 | 端到端演练工具链；压缩空间有限，所以结果不惊艳但真实 |
| 第二批 | LibreFace ResNet-18 | 90 MB、24 倍大；压缩比的量级在这里才可能出现，头条数字也来自这里 |

第一轮是工具链的彩排，不是最大压缩能力的展示。它先做的理由是：**工具链的风险必须用最低成本探明。**

### 5.3 阶段划分

| 阶段 | 内容 | 出口判据 |
|---|---|---|
| 0 | 量化探针 | 三问均有证据支撑的结论 |
| 1 | 最小量化实验 | FP32 与 INT8 的延迟、体积、精度三个实测数 |
| 2 | 感知管线模块化 | 新 `dash/` 包与 `02_realtime.py` 行为等价；各模块可单独运行 |
| 3 | 平滑层（滑窗、再 Kalman）与光流 | 抖动下降，且用量化指标证明 |
| 4 | 完整量化实验与结构化剪枝 | 延迟-精度曲线、多方法对比表 |
| 5（可选） | 身体动捕到骨骼映射 | 关键点到关节角度的 demo 与可视化 |

阶段 5 是唯一可砍项。其余各项都指向面试就绪线。

### 5.4 提前固定的对比协议

为保证数字可解释，所有量化对比遵守以下规则：

1. **同侧比较。** ONNX FP32 对 ONNX INT8，绝不拿 TFLite FP32 对 ONNX INT8。否则转换误差与量化误差无法分离。
2. **同一批输入。** 两个模型使用完全相同的校准数据与评估数据、相同的预热次数。
3. **不用单个输入下判定。** 单次随机输入配合阈值等于掷硬币：本管线实测翻转率 33%。应使用固定种子与多个输入，并同时报告中位数与最坏值。
4. **报告实测值。** 若实测数字不如申报目标，如实写出并给出原因。

---

## 六、方法论发现

以下是实验过程中发现的问题。列出它们，一是因为可复用，二是因为每一条都推翻了一个更早的结论。

### 6.1 欠定的线性系统会静默过拟合

从 1434 维输入映射到 52 维输出的线性模型，每个输出有 1434 个未知数，而训练样本只有 1200 个。系统**欠定**，因此训练集上存在精确解。最小二乘求解器返回了其中一个，得到训练集 R² 为 1.0，验证集 R² 为 0.74。

教训是：**不做容量控制，架构比较测的是"哪个模型过拟合更少"，而不是"哪个模型更好"。** 加入 L2 正则化后，同一数据上的验证集 R² 从 0.74 升到 0.95。

### 6.2 随机划分会跨时间泄露

数据集是同一段录像里连续的 1500 帧。相邻帧的平均绝对差异，只有相隔很远两帧差异的 **1/21**。因此随机抽取验证集，等于把训练帧的近重复样本放进验证集。

同一模型上的实测：

| 划分方式 | 验证集 R² |
|---|---|
| 随机 80/20 | 0.9517 |
| 按时间前 80% / 后 20% | 0.6314 |
| 按时间前 50% / 后 50% | 0.5821 |

**真实泛化能力无法从该数据集确定。** 它需要按时间划分，或者更好——另录一段独立数据。这是**数据采集问题，不是建模问题**。

### 6.3 单一数据源限制了可学的内容

52 个 BlendShape 通道中有 14 个在录制中从未被激活（标签标准差低于 0.01）。这些通道学不到，其对应的权重列也塌缩到接近零。在任何精度声明有意义之前，必须先有覆盖全部通道的表情录制方案与跨受试者数据。

### 6.4 INT8 量化在这个对象上有可测的弱点

对拟合的权重矩阵做量化，并测量单次前向的影响：

| 配置 | 最大误差 | 平均误差 |
|---|---|---|
| 浮点参考 | 0 | 0 |
| 仅量化权重 | 0.239 | 0.068 |
| 仅量化输入 | 0.086 | 0.010 |
| 两者都量化 | 0.248 | 0.072 |

作为对照，模型自身相对标签的平均绝对误差是 0.0155。**量化误差是模型误差的数倍**，因此对这个对象直接做朴素的 per-tensor 训练后量化会把它毁掉。

识别出三个成因：

1. **权重重尾。** 最大值与标准差之比为 14.2，而高斯分布约为 4。per-tensor 的 scale 由极值决定，其余权重因此损失分辨率。改用 per-channel 权重量化后，逐列误差中位数改善 **3.9 倍**。
2. **累加项数多。** 每个输出求和 1434 个乘积，独立舍入误差按 1434 的平方根增长，约 38 倍。
3. **大量抵消。** 单项乘积的量级约 0.072，而输出量级只有 0.052。输出比它自己的各项还小，说明求和依赖近乎精确的抵消，而量化恰好扰动这一点。

累加本身不是问题：INT8 矩阵乘在 **INT32 中精确累加**，只在最后舍入一次。1434 项、每项最多 127 乘 127，累加器峰值为 23,128,986，距 INT32 上限尚有 93 倍余量。**误差不会在一层的加法中累积。**

对计划的影响是具体的，而且风险清单中早有预判：先试动态量化；若精度退化，再上带校准集的静态量化，或让敏感层保留更高精度。同时，这也是一个**可验证的假设**——用梯度下降训练出的网络，其权重分布应当比闭式最小二乘解更温和，因而量化表现更好。

### 6.5 其他

| 发现 | 详情 |
|---|---|
| 模型文件部分为 float16 | 占比 30% / 20% / 35%，这决定了转换误差的下限在 1e-4 附近 |
| 由单个输入得出的判定不稳定 | 误差散布 5.15e-05 到 1.53e-04，判据 1e-4 落在分布中间，翻转率 33% |
| `mediapipe` 的 `close()` 会阻塞 | 间歇性在 `executor.shutdown(wait=True)` 处等待 42 秒 |
| 延迟测量噪声 | 重复运行下均值约 5%，p95 约 11.6% |

---

## 七、未决问题与风险

| 问题 | 影响 | 应对 |
|---|---|---|
| 数据集为单受试者、单次录制 | 精度声明尚不具意义 | 补录更多场次与受试者；过渡期按时间划分 |
| 52 个表情通道中 14 个未激活 | 模型无法学习这些通道 | 扩展录制方案以覆盖全部通道 |
| INT8 精度可能崩溃 | 核心交付物受威胁 | 先动态量化，再带校准集的静态量化，最后混合精度 |
| `tflite2onnx` 的工具链 bug | 路线 A 只能部分走通 | 工作由候选 B 承担；把 bug 本身作为发现写进报告 |
| 当前瓶颈在摄像头而非推理 | 现有管线上看不到量化收益 | 在高分辨率或多模型配置下测量，并明确说明这一点 |
| 文档存放在 git 仓库之外 | 无版本历史、无备份 | 决定是否把 notes 目录纳入版本控制 |
| 长周期并行项尚未启动 | 会阻塞后续阶段 | 现在就启动模型授权注册与数据集申请，因为它们是纯等待时间 |

---

## 八、下一步

第一批工作内的即时项：

1. 完成 `quant/train_mlp.py`：训练关键点到 BlendShape 的映射并导出 ONNX
2. 验证导出的 ONNX 与 PyTorch 在同一批输入上的输出一致（约 1e-5 量级）
3. 写出决策门文档，记录三问结论、路线选择与实际耗时
4. 执行训练后量化，产出 INT8 模型
5. 产出三方对比：延迟、体积、精度，以表格加 CSV 形式

不阻塞上述工作的并行项：

6. 补录一轮数据，使用更完整的表情方案，使精度数字变得有意义
7. 启动长周期行政项（模型授权注册、数据集申请），因为它们是纯等待时间

---

## 九、附录

### 9.1 仓库结构

```
D:\DASH\V0FastTest\
├── deploy/                      本地推理脚本（单图、实时）
├── quant/                       量化工作
│   ├── bench.py                 延迟与体积度量
│   ├── probe_unpack.py          问题一：容器拆解
│   ├── probe_route_a.py         问题二-A：TFLite 转 ONNX
│   ├── collect_pairs.py         问题二-B：数据集采集
│   ├── preview_pairs.py         数据集验证与作图
│   ├── train_mlp.py             问题二-B：训练与导出（进行中）
│   ├── data/                    数据集（不入库）
│   ├── models/                  拆出与转换的模型（不入库）
│   ├── result/                  证据记录
│   └── tests/                   度量工具的测试
├── fusion/                      双源融合服务器
├── viewer/                      three.js 加 VRM 渲染器
├── SysMocap/, SysMocapApp/      身体动捕组件
├── facial-motion-capture/       面部捕捉组件
└── TECHNICAL_ROUTE_REPORT_*.md  本报告
```

实测产物被有意排除在版本控制之外：模型与数据集是产物，不是源码。脚本与证据记录纳入版本控制。

### 9.2 复现命令

```powershell
# 解释器固定
$py = "D:\Miniconda3\python.exe"

# 度量工具测试
& $py -m pytest

# 问题一：拆出内含模型
& $py -m quant.probe_unpack

# 问题二-A：尝试转换
& $py -m quant.probe_route_a

# 问题二-B：采集数据集（摄像头）
& $py -m quant.collect_pairs

# 验证数据集并重新生成图
& $py -m quant.preview_pairs

# 问题二-B：训练与导出
& $py -m quant.train_mlp
```

### 9.3 参考文献

底层感知模型：

| 模型 | 论文 |
|---|---|
| BlazePose | arXiv:2006.10204，CVPR 2020 Workshop |
| BlazeFace | arXiv:1907.05047 |
| Face Mesh | arXiv:1907.06724 |
| MediaPipe Hands | arXiv:2006.10214，CVPR 2020 Workshop |
| MediaPipe 框架 | arXiv:1906.08172，CVPR 2019 Workshop |
| MediaPipe Holistic | 无正式论文；出处为 Google Research 博客与 AI Edge 文档 |

引用勘误：本笔记早期版本曾把 MediaPipe Holistic 引为 arXiv:2012.07808。该编号实际属于一篇无关的自然语言处理论文。MediaPipe Holistic 没有正式发表。对其手部区域检测缺陷的学术讨论见 arXiv:2405.03545。

软件：

| 项目 | 许可 |
|---|---|
| SysMocap | MPL-2.0 |
| miniface（facial-motion-capture） | MIT-Attribution |
| Kalidokit | 社区开源库，无正式论文 |
